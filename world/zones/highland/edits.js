// Highland — World Editor (Layer 4) data. Same schema as lagoon/edits.js
// (see core/world-edits.js's header) — empty, same structural-uniformity
// reason as highland/bindings.js (its own scatterRecipe is still `held`).
export const edits = {
  placed: [],
  familyOverrides: {},
  scatterEdits: {},
};
