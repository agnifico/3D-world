// Grassland — World Editor (Layer 4) data. Same schema as lagoon/edits.js
// (see core/world-edits.js's header) — empty until the editor (or Agni by
// hand) places something here; wired into build() regardless, for the same
// structural-uniformity reason grassland/bindings.js's row shape matches
// every other zone's.
export const edits = {
  placed: [],
  familyOverrides: {},
  scatterEdits: {},
};
